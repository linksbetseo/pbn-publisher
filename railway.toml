[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = ["frontend/**", "backend/**", "Dockerfile"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
